platform :ios, '9.0'

target 'example-ios-travis' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for example-ios-travis
  pod 'SwiftHTTP', '~> 2.0'
  pod 'PaginatedScrollView', '~> 0.2'
  pod 'ContactCircularView', '~> 1.1'
  pod 'Compose', '~> 1.2'
  pod 'expanding-collection', '~> 1.1'

  target 'example-ios-travisTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'example-ios-travisUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
